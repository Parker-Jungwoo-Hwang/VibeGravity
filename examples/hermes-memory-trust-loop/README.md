# Hermes Memory Trust Loop Example

Run from the repo root:

```bash
go run ./cmd/cli eval demo
```

This example is local-only. It does not require PostgreSQL, Hermes, Codex, or
network access.

The expected output is in `expected-output.txt`.
